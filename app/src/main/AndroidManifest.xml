<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- Images permission is OPTIONAL - only needed for enhanced album art display -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" android:required="false" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Optional permission for auto-update installation from GitHub -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" android:required="false" />
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <!-- Bluetooth permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" 
                     android:usesPermissionFlags="neverForLocation"
                     tools:targetApi="s" />

    <!-- USB Host permissions for exclusive USB DAC access -->
    <uses-permission android:name="android.permission.MANAGE_USB" tools:ignore="ProtectedPermissions" />
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />

    <queries>
        <intent>
            <action android:name="android.media.browse.MediaBrowserService" />
        </intent>
        <intent>
            <action android:name="androidx.media3.session.MediaSessionService" />
        </intent>
    </queries>

    <!-- Attribution tags to silence AppOps "attributionTag not declared" errors -->
    <attribution android:tag="media_playback" android:label="@string/attribution_media_playback" />
    <attribution android:tag="usb_audio" android:label="@string/attribution_usb_audio" />
    <attribution android:tag="playback" android:label="@string/attribution_media_playback" />
    <attribution android:tag="siphon" android:label="@string/attribution_usb_audio" />

    <application
        android:name=".RhythmApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:localeConfig="@xml/locale_config"
        android:theme="@style/Theme.Rhythm"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="36">
        
        <!-- Media3 notification icon -->
        <meta-data
            android:name="androidx.media3.ui.PlayerNotificationManager.smallIcon"
            android:resource="@drawable/ic_notification" />
        <meta-data
            android:name="com.google.android.gms.car.notification.SmallIcon"
            android:resource="@drawable/ic_notification" />
        
        <!-- FileProvider for APK installation -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        
        <!-- Add MediaTransferReceiver for Output Switcher functionality -->
        <receiver
            android:name="androidx.mediarouter.media.MediaTransferReceiver"
            android:exported="true">
        </receiver>

        <!-- USB Audio Receiver — triggers OS 'Open Rhythm to handle USB-C Audio?' dialog -->
        <receiver
            android:name=".infrastructure.audio.UsbAudioReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
                <action android:name="android.hardware.usb.action.USB_DEVICE_DETACHED" />
                <action android:name="chromahub.rhythm.app.USB_PERMISSION" />
            </intent-filter>
            <!-- This metadata tells Android to only trigger the dialog for USB audio class devices -->
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/usb_device_filter" />
        </receiver>
        
        <!-- Music Widget Receiver (Legacy RemoteViews) -->
        <receiver
            android:name=".infrastructure.widget.MusicWidgetProvider"
            android:exported="true"
            android:label="Rhythm Music (Legacy)">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="chromahub.rhythm.app.infrastructure.widget.PLAY_PAUSE" />
                <action android:name="chromahub.rhythm.app.infrastructure.widget.SKIP_NEXT" />
                <action android:name="chromahub.rhythm.app.infrastructure.widget.SKIP_PREVIOUS" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>
        
        <!-- Rhythm Music Widget (Modern Glance with Material 3) -->
        <receiver
            android:name=".infrastructure.widget.glance.RhythmWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_name_glance"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info_glance" />
        </receiver>
        
        <service
            android:name=".infrastructure.service.MediaPlaybackService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
                <action android:name="android.media.browse.MediaBrowserService" />
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
            <meta-data
                android:name="androidx.media3.ui.PlayerNotificationManager.smallIcon"
                android:resource="@drawable/ic_notification" />
            <meta-data
                android:name="androidx.media3.session.MediaNotification.smallIcon"
                android:resource="@drawable/ic_notification" />
            <meta-data
                android:name="androidx.media3.ui.NotificationUtil.smallIcon"
                android:resource="@drawable/ic_notification" />
            <meta-data
                android:name="androidx.media3.session.DefaultMediaNotificationProvider.smallIcon"
                android:resource="@drawable/ic_notification" />
            <meta-data
                android:name="androidx.media3.ui.notification.small_icon"
                android:resource="@drawable/ic_notification" />
            <meta-data
                android:name="androidx.media3.session.notification.small_icon"
                android:resource="@drawable/ic_notification" />
        </service>

        <receiver
            android:name=".infrastructure.service.MediaButtonIntentReceiver"
            android:exported="true">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>
        
        <activity
            android:name=".activities.CrashActivity"
            android:exported="false"
            android:theme="@style/Theme.Rhythm"
            android:process=":crash_process" />

        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.App.Starting"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|uiMode|keyboard|keyboardHidden|density">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.APP_MUSIC" />
            </intent-filter>
            
            <!-- USB device filter for auto-launch on DAC connect -->
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"/>
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/usb_device_filter"/>
            
            <!-- Handle playlists -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="audio/x-mpegurl" />
                <data android:mimeType="audio/mpegurl" />
                <data android:mimeType="application/x-mpegurl" />
                <data android:mimeType="application/vnd.apple.mpegurl" />
                <data android:mimeType="application/vnd.apple.mpegurl.audio" />
            </intent-filter>
        </activity>
        
        <!-- External playback activity for quick audio file playback -->
        <activity
            android:name=".activities.ExternalPlaybackActivity"
            android:exported="true"
            android:label="Play Audio"
            android:theme="@android:style/Theme.DeviceDefault.Dialog.NoActionBar"
            android:launchMode="singleTask"
            android:excludeFromRecents="false"
            android:configChanges="orientation|screenSize|keyboardHidden">
            
            <!-- Handle VIEW intents for audio files -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="audio/*" />
            </intent-filter>
            
            <!-- Handle specific audio file types -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="audio/mpeg" />
                <data android:mimeType="audio/mp3" />
                <data android:mimeType="audio/x-mp3" />
                <data android:mimeType="audio/mp4" />
                <data android:mimeType="audio/m4a" />
                <data android:mimeType="audio/aac" />
                <data android:mimeType="audio/x-aac" />
                <data android:mimeType="audio/ogg" />
                <data android:mimeType="audio/flac" />
                <data android:mimeType="audio/wav" />
            </intent-filter>
        </activity>
    </application>

</manifest>
